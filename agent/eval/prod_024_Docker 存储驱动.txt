# Docker 存储驱动

Docker 镜像分层存储——每个 RUN/COPY/ADD 指令创建一个新层。存储驱动管理层——overlay2(推荐,两个目录 lower/upper + work 合并)、aufs(废弃)、devicemapper、btrfs、zfs。Copy-on-Write——修改只影响上层不影响底层。容器层为可写层，删除容器即丢弃。镜像层只读且共享多容器复用。写时复制策略减少磁盘空间占用。

分类: 容器与编排