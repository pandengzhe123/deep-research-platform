Docker is a platform for developing, shipping, and running applications in containers. Containers are lightweight, isolated environments that package code and dependencies together. Docker images are built from Dockerfiles, which specify the base image, dependencies, and commands. Docker Compose allows defining multi-container applications in a single YAML file. Kubernetes is a container orchestration platform that extends Docker's capabilities for large-scale deployments.