# Vector Clock

向量时钟(Vector Clock)在分布式系统中追踪事件的因果关系。每个节点维护一个 N 维向量[N1,N2,...,Nn]，Ni 表示节点 i 的事件计数。每次本地事件增加自己的计数器。发送消息时附带本地向量时钟。接收方合并向量时钟——取每个维度的最大值。版本冲突检测——两个向量时钟不可比较(concurrent)表示存在冲突。Dynamo 和 Riak 用向量时钟解决多副本写入冲突。与 Lamport Clock 的区别——向量时钟可判断并发而 Lamport Clock 不能。

分类: 分布式系统